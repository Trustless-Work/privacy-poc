[package]
name = "circuit_spender_transfer_full_release"
type = "bin"
authors = ["OpenZeppelin", "Trustless Work"]
compiler_version = ">=0.30.0"

[dependencies]
stellar_confidential_lib = { path = "../lib" }
