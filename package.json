{
  "name": "@trustless-work/privacy-poc",
  "version": "0.0.0",
  "private": true,
  "description": "Testnet PoC for confidential USDC milestone escrow on Stellar",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
